# How to Run Backend Foundly
`npm install` to install the package
make sure you already installed node.js
`npx prisma generate` for restart/or read schema.prisma file
`npx prisma db push` for migration all of the model/table to the database
